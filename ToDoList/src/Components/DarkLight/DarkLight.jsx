export default function DarkLight() {
    return (
        <button>light</button>
    )
}

// Idea para el modo oscuro y claro
/**
 * let mode;
 * if (button=true) {
 * mode= <Darkmode/>
 * } esle {
 * mode= <Lightmode/>
 * }
 * return (
 * {mode}
 * )
 * 
 *  o lo mismo con ternaria:
 * let button = 'true' ? {Lightmode} : {Darkmode}
 */